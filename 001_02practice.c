#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("area=%f\n", pi * radius * radius);
    int h = 3;
    printf("volume of cyl is %f", pi * radius * radius * h);
    return 0;
}